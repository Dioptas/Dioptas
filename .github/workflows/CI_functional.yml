# This is a basic workflow to help you get started with Actions

name: CI_functional

on: [push, pull_request]

jobs:
  test:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v3
    - run: git fetch --prune --unshallow
    - name: Setup Python 3.9
      uses: actions/setup-python@v3
      with:
        python-version: '3.9'

    - name: Use Python Dependency Cache
      id: pip-cache
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('Dioptas.spec') }}-c3
        restore-keys: |
          ${{ runner.os }}-pip-c3

    - name: Install Apt Dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update -y
        sudo apt-get install libegl1-mesa libgl1-mesa-glx xserver-xorg-video-dummy libxkbcommon-x11-0 libxkbcommon0 \
          libxkbcommon-dev libxcb-icccm4 libxcb-image0 libxcb-shm0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 \
          libxcb-render0 libxcb-shape0 libxcb-sync1 libxcb-xfixes0 libxcb-xinerama0 libxcb-xkb1 libxcb-cursor0 libxcb1 libopengl0

    - name: Install Pip Dependencies
      run: |
        pip install pycifrw pandas python-dateutil h5py scikit-image pyqt6 cython future qtpy pyfai lmfit \
                    pyepics extra_data pyinstaller watchdog pyopengl pyopengl-accelerate h5py hdf5plugin pyqtgraph \
                    mock pytest pytest-xvfb pytest-qt

    - name: Start X server
      if: runner.os == 'Linux'
      run: |
        export DISPLAY=:99.0
        Xorg -noreset +extension GLX +extension RANDR +extension RENDER -logfile ./99.log -config ./.github/workflows/xorg.conf :99 &
        sleep 3

    - name: Run tests
      run: |
        py.test dioptas/tests/functional_tests/test_functional_JcpdsEditor.py
        py.test dioptas/tests/functional_tests/test_img_bg.py
        py.test dioptas/tests/functional_tests/test_userinterface.py
        py.test dioptas/tests/functional_tests/test_functional_integration.py
